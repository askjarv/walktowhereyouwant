// Fitbit API Configuration
window.config = {
    // This file is a template. The actual values are injected by GitHub Actions during deployment.
    // See .github/workflows/deploy.yml for details.
    CLIENT_ID: 'YOUR_CLIENT_ID',
    // The redirect URI must match exactly what you registered in your Fitbit app settings
    REDIRECT_URI: 'https://askjarv.github.io/walktowhereyouwant/callback.html'
}; 
